{
  "//": "INTENTIONALLY EMPTY — DO NOT ADD ANYTHING HERE. All Claude Code configuration for this machine is unified in ~/.claude/settings.json (permissions, hooks, env, sandbox). This project-level file is deliberately kept empty; never write permissions, hooks, or env into it. To change config, edit the profile at ~/.claude/settings.json instead."
}
